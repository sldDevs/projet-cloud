apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: dmz-network
  namespace: saliou-diop-dev
spec:
  config: '{
      "cniVersion": "0.3.1",
      "name": "dmz-network",
      "type": "ovn-k8s",
      "topology": "layer2",
      "netconf": { "mtu": 1500 }
    }'
---
apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: wan-network
  namespace: saliou-diop-dev
spec:
  config: '{
      "cniVersion": "0.3.1",
      "name": "wan-network",
      "type": "ovn-k8s",
      "topology": "layer2",
      "netconf": { "mtu": 1500 }
    }'